FROM alpine:3.21

RUN apk add --no-cache ca-certificates tzdata
RUN addgroup -S sherlock && adduser -S sherlock -G sherlock

COPY sherlock /usr/local/bin/sherlock
COPY migrations /migrations

USER sherlock

EXPOSE 8080

ENTRYPOINT ["sherlock"]
CMD ["serve"]
