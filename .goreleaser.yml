version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: sherlock
    main: ./cmd/sherlock
    binary: sherlock
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - id: sherlock
    name_template: "sherlock_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

dockers:
  - image_templates:
      - "ghcr.io/rahul-kosamkar/sherlock:{{ .Version }}"
      - "ghcr.io/rahul-kosamkar/sherlock:latest"
    dockerfile: Dockerfile.goreleaser
    extra_files:
      - migrations
    build_flag_templates:
      - "--platform=linux/amd64"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: rahul-kosamkar
    name: sherlock
  prerelease: auto
